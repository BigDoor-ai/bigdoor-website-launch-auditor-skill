# Contributing

Good contributions include better checklists, safer audit guidance, agent adapters, deterministic CLI checks, and sample reports.

Rules:

- Do not claim the tool guarantees compliance.
- Do not add unsafe instructions.
- Mark unverifiable checks as `[Unverified]`.
- Keep the project agent-agnostic.
